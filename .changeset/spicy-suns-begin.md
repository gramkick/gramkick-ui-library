---
"@gramkick/ui": minor
---

Initial release: design tokens (`theme.css` / prebuilt `styles.css`), `cn` helper,
`useMediaQuery` / `useControllableState` hooks, and the first components —
`Button`, `Badge`, `Card`, `Input`, `Spinner`, and a Radix-based `Dialog`.
