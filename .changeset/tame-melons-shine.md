---
"@gramkick/ui": minor
---

`Dropdown` and `Autosuggest` gain a `maxHeight` prop to cap the scrollable options list's height (number = px, string used as-is, e.g. `"50vh"`). Default stays `320` (px), matching prior behavior.
